-- 1. Permite que qualquer pessoa insira novos registros (essencial para o checkout)
create policy "Permitir inserção anônima" 
on public.produto_cackto 
for insert 
to anon 
with check (true);

-- 2. (Opcional) Se quiser que você consiga ver os dados no seu site depois:
-- create policy "Permitir leitura para todos" on public.produto_cackto for select using (true);
