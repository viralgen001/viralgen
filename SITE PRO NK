create table usuarios_verificados (
  id uuid default gen_random_uuid() primary key,
  nome_completo text,
  email text,
  celular text,
  cpf text,
  indicacao text,
  criado_em timestamp with time zone default now()
);
